(function () {
    "use strict";

    //angular.module('$app_Later$', []);
    angular.module('$app$')
        .factory('$serviceName$', $serviceName$);

    $serviceNameInject$.$inject = ['$injectable$'];

    function $serviceName$($injectable$) {

        var service = {
            $func$: $func$
        };

        return service;

        ////////////

        function $func$() {
            $END$
        }
    }

})();